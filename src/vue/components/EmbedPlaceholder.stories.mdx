import { Meta, Story } from '@storybook/addon-docs/blocks';
import { EmbedPlaceholder } from './EmbedPlaceholder';
import { EmbedPlaceholderFacebook } from './EmbedPlaceholderFacebook';
import { EmbedPlaceholderInstagram } from './EmbedPlaceholderInstagram';
import { EmbedPlaceholderTwitter } from './EmbedPlaceholderTwitter';
import { EmbedPlaceholderYoutube } from './EmbedPlaceholderYoutube';

<Meta title='Embeds | Placeholder' components={[EmbedPlaceholder]} />

# Embed Placeholder

## Generic

This component is used as a generic social media embed placeholder. Furthermore it's the base component for our vendor specific placeholder components.

<Story name='Generic'>{{
  components: { EmbedPlaceholder },
  template: `<EmbedPlaceholder @requestConsent="requestConsent" />`,
  methods: {
    requestConsent() {
        window.alert('Request Consent');
    }
  }
}}></Story>

## Facebook

This placeholder is used for embeds that contains content from Facebook.

<Story name='Facebook'>{{
  components: { EmbedPlaceholderFacebook },
  template: `<EmbedPlaceholderFacebook @requestConsent="requestConsent" />`,
  methods: {
    requestConsent() {
      window.alert('Request Consent');
    }
  }
}}></Story>

## Instagram

This placeholder is used for embeds that contains content from Instagram.

<Story name='Instagram'>{{
  components: { EmbedPlaceholderInstagram },
  template: `<EmbedPlaceholderInstagram @requestConsent="requestConsent" />`,
  methods: {
    requestConsent() {
      window.alert('Request Consent');
    }
  }
}}></Story>

## Twitter

This placeholder is used for embeds that contains content from Twitter.

<Story name='Twitter'>{{
  components: { EmbedPlaceholderTwitter },
  template: `<EmbedPlaceholderTwitter @requestConsent="requestConsent" />`,
  methods: {
    requestConsent() {
      window.alert('Request Consent');
    }
  }
}}></Story>

## Youtube

This placeholder is used for embeds that contains content from Youtube.

<Story name='Youtube'>{{
  components: { EmbedPlaceholderYoutube },
  template: `<EmbedPlaceholderYoutube @requestConsent="requestConsent" />`,
  methods: {
    requestConsent() {
      window.alert('Request Consent');
    }
  }
}}></Story>
